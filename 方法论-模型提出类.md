您好，请扮演一位资深的人工智能研究员（专注于CV与AIGC方向）。接下来我将提供一篇学术论文，请您在仔细阅读并理解其内容的基础上，严格依据论文原文，详细、清晰地回答以下几个板块的问题。

在回答时，请在每个信息点后附上对应的引用标记（如 [Page X] 或 [Source X]）。

### 1. 核心创新与背景 (Core Novelty)
* **核心贡献 (Contribution)：** 这篇论文试图解决什么核心痛点？它提出的核心技术贡献是什么？
* **与 SOTA 对比 (Comparison)：** 与该领域之前的最先进方法（State-of-the-Art）相比，本文的方法主要改进了哪里？（例如：是架构更高效、生成质量更好、还是模态对齐更准？）
* **关键术语 (Key Terms)：** 请详细解释论文中提出的、对于理解其贡献至关重要的关键创新术语或概念。

### 2. 模型架构与机理 (Architecture & Mechanism)
* **网络架构 (Network Architecture)：** 请详细说明模型的整体网络架构。如果是多模态模型，请说明不同模态的编码器（Encoder）和解码器（Decoder）是如何交互的。
* **数据流向 (Data Flow)：** 请按照数据在模型中处理的先后顺序，一步步描述数据从输入端开始，经过了哪些主要模块（Block）或层（Layer），在每个模块中数据发生了什么变化（如维度变化、特征融合、注意力机制计算等），直到生成最终输出。
* **数学原理 (Math/Formulation)：** (如果适用) 请简要描述模型核心组件背后的数学原理或公式逻辑（例如扩散过程、注意力计算方式、损失函数设计）。

### 3. 输入输出与特征空间 (Input/Output & Latent Space)
* **具体输入 (Inputs)：** 模型的具体输入是什么？（请说明图像分辨率、文本Token长度、Prompt格式、Conditioning条件等）。
* **潜在空间/特征表示 (Latent Representation)：** (针对AIGC/多模态) 模型是否使用了潜在空间（Latent Space）？如果有，这个空间的特征表示具有什么关键特性（如解耦性、连续性）？它是如何生成的？
* **最终输出 (Outputs)：** 模型的最终输出是什么？（例如：去噪后的图像、预测的噪声、3D点云、特征向量等）。

### 4. 训练与消融 (Training & Ablation)
* **损失函数 (Loss Functions)：** 请详细阐述用于训练模型的损失函数构成。每一项Loss具体负责约束什么？
* **消融实验 (Ablation Study)：** 论文通过消融实验证明了哪些组件或设计是必不可少的？（即：去掉哪部分会导致性能显著下降？）
* **训练策略 (Training Strategy)：** 是否存在特殊的训练策略？（例如：多阶段训练、课程学习、混合精度训练、数据增强策略等）。

### 5. 任务流程与推理 (Inference & Pipeline)
* **推理流程 (Inference Pipeline)：** 在模型训练完成后，从输入一个查询（如Text Prompt或Image）到获得最终结果，完整的推理工作流程是怎样的？（请特别注意采样步数、引导系数 Guidance Scale 等推理参数）。
* **应用场景 (Applications)：** 该模型被设计用于执行什么核心任务？（例如：文生图、图像编辑、多模态检索、检测等）。



1.具体说明从输入到输出的完整训练过程中，发生的数据维度变化等细节原理；并说明推理时输入与训练时输入的区别。
2.训练的数据是如何得来的，哪些是开源数据；训练数据的格式是怎么样的。
3.作者在这个工作中，哪些模块或者策略是自己独特提出的；哪些是已经存在但是未使用在video-relight领域的；哪些是已经存在且已经用在video-relight中，仅拿过来缝合使用的